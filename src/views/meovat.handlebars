
<div class="section-title">
			<h2>Mẹo, thủ thuật và kĩ thuật</h2>
		</div>
<div class="mt-4">
    <div class="row">
    {{#each user}}
    <div class="col-sm-6 col-lg-4" style="margin: 10px 0px;">
      <div class="card card-course-item" data-slug="{{this.SLUG}}" data-name="{{this.NAME}}" data-image="{{this.IMAGE}}" style="height: 600px;">
        <a href="/MeoVat/{{this.SLUG}}">
          <img class="card-img-top" style="height: 300px;" src="{{this.IMAGE}}" alt="{{this.NAME}}">
        </a>
        <div class="card-body">
          <a href="/MeoVat/{{this.SLUG}}">
            <h5 class="card-title">{{this.NAME}}</h5>
          </a>
          <p class="card-text">{{this.NOI_DUNG}}.</p>
          <div class="blockheart" data-slug="{{this.SLUG}}" data-name="{{this.NAME}}" data-image="{{this.IMAGE}}">
            <div class="heart"></div>
            <div class="animation-heart"></div>
          </div>
        </div>
    </div>
</div>
{{/each}}
  <script src="/js/favourite-trick.js"></script>

<ul class="block-navigate">
  <li class="item-navigate item-navigate--active">
	@@ -35,4 +34,3 @@
  </li>
</ul>

<script src="/js/favourite-trick.js"></script>
